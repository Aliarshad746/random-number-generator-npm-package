## What is this?

A random number generator

## Installation

Run `npm i rng_arshad`

Use:

```
import rng from 'rng_arshad';

rng(5, 10);
```

## Parameters

The Ranndom Number Generator accepts two parameters, min and max values.

By default, `min` is set to 0, and `max` is set to 100.
